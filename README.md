# gaudi
Gaudi analyzes a repository’s Python/JS/TS source, extracts definitions and references, ranks them by structural importance, and generates a small `.map` file containing the most useful signatures and file-level cues. AI agents read that map first to form a high-value mental model, then inspect the real source files for implementation details.
